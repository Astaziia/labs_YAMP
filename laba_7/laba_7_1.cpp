#include <iostream>
#include <vector>
using namespace std;

/*int main() {
    vector<int> positives;
    int num;

    while (cin >> num && num != 0) {
        if (num > 0) {
            positives.push_back(num);
        }
    }

    cout << "Count of positive numbers: " << positives.size() << endl;
    return 0;
}*/
