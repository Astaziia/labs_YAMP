#include <iostream>
#include <vector>
using namespace std;

/*int main() {
    vector<int> even, odd;
    int num;

    while (cin >> num && num != 0) {
        if (num % 2 == 0)
            even.push_back(num);
        else
            odd.push_back(num);
    }

    cout << "Even numbers: ";
    for (int n : even) cout << n << " ";
    cout << "\nCount: " << even.size() << endl;

    cout << "Odd numbers: ";
    for (int n : odd) cout << n << " ";
    cout << "\nCount: " << odd.size() << endl;

    return 0;
}*/
